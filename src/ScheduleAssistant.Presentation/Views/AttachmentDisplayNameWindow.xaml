<Window x:Class="ScheduleAssistant.Presentation.Views.AttachmentDisplayNameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="修改附件显示名"
        Width="520"
        Height="220"
        MinWidth="420"
        Background="{DynamicResource Brush.WindowBackground}"
        Foreground="{DynamicResource Brush.TextPrimary}"
        FontFamily="{DynamicResource FontFamily.App}"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="{DynamicResource Spacing.Window}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Style="{StaticResource Text.SectionTitle}" Text="修改附件显示名" />
        <TextBox x:Name="DisplayNameTextBox"
                 Grid.Row="1"
                 Margin="0,14,0,0"
                 AutomationProperties.Name="附件显示名"
                 Style="{StaticResource TextBox.Base}" />
        <StackPanel Grid.Row="3" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Margin="0,0,8,0"
                    AutomationProperties.Name="取消重命名"
                    Click="OnCancelClick"
                    Content="取消"
                    IsCancel="True"
                    Style="{StaticResource Button.Secondary}" />
            <Button AutomationProperties.Name="确认重命名"
                    Click="OnConfirmClick"
                    Content="确认"
                    IsDefault="True"
                    Style="{StaticResource Button.Primary}" />
        </StackPanel>
    </Grid>
</Window>
