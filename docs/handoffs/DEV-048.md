# DEV-048 Presentation–Domain 架构边界修复

## 任务范围与基线

- 任务 ID：`DEV-048`。
- 目标：消除 `ScheduleAssistant.Presentation` 对 `ScheduleAssistant.Domain` 的程序集引用，并保持既有任务、查询、排序、完成状态和紧迫度语义不变。
- 工作分支：`integration/dev-041-dev-042-core-ui`。
- 工作 worktree：`E:\Dev\Personal\Todo_list.worktrees\integration-dev-041-dev-042`。
- 允许修改目录：`src/ScheduleAssistant.Application/Tasks`、`src/ScheduleAssistant.Application/Calendar`、`src/ScheduleAssistant.Presentation/ViewModels`、对应 Application/Presentation 测试目录及 `docs/handoffs`。
- 明确不在范围：修改 `codex/dev-043-ui-refinement`、`main`、Infrastructure 仓储/SQL、Domain 模型、数据库表或 migration、架构测试规则、无关 UI 或 SPIKE。
- 已保留分支上的 `7b495d9` 文档提交及未跟踪 `NUL`，没有改写或删除。

## 完成内容与设计选择

- Application 新增 `TaskPriorityCode`、`WorkflowStatusCode`、`DisplayStatusCode` 与 `DeadlineUrgencyCode`，显式保留 Domain 枚举的数值语义。
- 新增 `TaskContractMapper` 作为唯一的 Domain↔Application 代码映射边界；创建/编辑优先级、搜索过滤器、TaskDto 和 CalendarEntry 均在 Application 内部完成映射。
- Deadline resolver 对 Presentation 返回 `DeadlineDto`，Domain `ZonedDeadline` 只留在 Application 内部的解析结果中。
- Presentation 的任务编辑器、任务卡、卡片映射器、今天页和 Deadline 倒计时均改用 Application 契约代码，移除所有 `using ScheduleAssistant.Domain`。
- 新增映射器双向覆盖测试，覆盖所有合法 Domain 与 Application 成员；未复制 Deadline 紧迫度、展示状态计算逻辑。

## 修改文件与接口/数据影响

- Application：任务/日历契约、查询过滤转换、用例创建/更新/查询映射、Deadline resolver，以及新增契约码和映射器。
- Presentation：五个指定 ViewModel/mapper 文件及其行为测试适配。
- Tests：新增 `TaskContractMapperTests`，更新 Application/Presentation 行为测试断言和构造数据。
- 数据库与 Infrastructure：无修改；Domain 枚举、存储编码和 migration：无修改。
- Presentation 对外程序集引用：仅 Application 与 Infrastructure；不新增 Domain 项目引用。

## 验证命令与结果

使用仓库配置的 SDK：`E:\Dev\Tools\ScheduleAssistantDotnet\dotnet.exe`。

- `& 'E:\Dev\Tools\ScheduleAssistantDotnet\dotnet.exe' build .\ScheduleAssistant.sln -c Release --no-restore`：通过，0 警告、0 错误。
- `& 'E:\Dev\Tools\ScheduleAssistantDotnet\dotnet.exe' test .\tests\ScheduleAssistant.Architecture.Tests\ScheduleAssistant.Architecture.Tests.csproj -c Release --no-build --no-restore`：通过，4 passed / 0 failed / 0 skipped。
- `& 'E:\Dev\Tools\ScheduleAssistantDotnet\dotnet.exe' test .\tests\ScheduleAssistant.Application.Tests\ScheduleAssistant.Application.Tests.csproj -c Release --no-build --no-restore`：通过，27 passed / 0 failed / 0 skipped。
- `& 'E:\Dev\Tools\ScheduleAssistantDotnet\dotnet.exe' test .\tests\ScheduleAssistant.Presentation.Tests\ScheduleAssistant.Presentation.Tests.csproj -c Release --no-build --no-restore`：通过，20 passed / 0 failed / 0 skipped。
- `git diff --check`：通过。

## 未完成项与后续门禁

- 需要将本地 DEV-048 提交与既有 `7b495d9` 一并推送到现有 PR #12 整合分支；不创建第二个 PR。
- 推送后等待 PR #12 CI 全部通过并补录链接；不合并。
- 用户仍需完成最终 WPF 人工复测；在人工验收前不宣称产品验收完成。

## 最终提交 SHA

- DEV-048 提交：`948f3ce`。
