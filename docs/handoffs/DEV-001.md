# DEV-001 交付记录

## 完成内容

- 创建 `ScheduleAssistant.sln` 以及 Domain、Application、Infrastructure、Presentation 四层项目。
- 创建 Domain、Application、Infrastructure 和 Architecture 四个 xUnit 测试入口，其中 Architecture 测试验证生产程序集依赖方向。
- 固定 `global.json` 中的 .NET SDK 10.0.100 feature band，并在 `Directory.Build.props` 与 `Directory.Packages.props` 集中配置编译、分析器和包版本。
- 在 WPF Presentation 项目中建立唯一组合根：使用 `Microsoft.Extensions.Hosting`/DI 创建 Host，注册当前 Infrastructure 边界和最小 `MainWindow`。
- 创建无业务数据的最小 WPF 启动页，仅用于确认应用组合根和窗口可以启动。
- 创建 `AGENTS.md`、README、规格索引、ADR 模板、handoff 模板、测试报告说明和 Windows GitHub Actions CI。

## 关键设计选择

- 目标框架采用当前规格日期下的稳定 LTS .NET 10：跨平台项目使用 `net10.0`，WPF 项目与架构测试使用 `net10.0-windows`。
- 通过 `EnableWindowsTargeting=true` 保留非 Windows 构建代理的编译可能性；实际 WPF 运行和 Windows API 冒烟仍限定在 Windows 10/11 x64。
- Presentation 对 Infrastructure 的引用只出现在组合根注册路径；View/ViewModel 尚未创建，也没有任何具体仓储、SQLite 或 Windows 适配器。
- 使用程序集 marker 作为当前空项目的最小可测试依赖锚点，避免为了满足架构测试而虚构业务接口或模型。
- `Directory.Packages.props` 预先集中登记 Host/DI、MVVM、SQLite、Dapper 和测试工具链版本；DEV-001 只实际引用 Host/DI 与 xUnit 测试包，未实现持久化。

## 修改文件

### 工程与规范

- `.editorconfig`
- `.gitignore`
- `global.json`
- `NuGet.config`
- `Directory.Build.props`
- `Directory.Packages.props`
- `ScheduleAssistant.sln`
- `README.md`
- `AGENTS.md`
- `.github/workflows/ci.yml`

### 源码

- `src/ScheduleAssistant.Domain/ScheduleAssistant.Domain.csproj`
- `src/ScheduleAssistant.Domain/AssemblyMarker.cs`
- `src/ScheduleAssistant.Application/ScheduleAssistant.Application.csproj`
- `src/ScheduleAssistant.Application/AssemblyMarker.cs`
- `src/ScheduleAssistant.Infrastructure/ScheduleAssistant.Infrastructure.csproj`
- `src/ScheduleAssistant.Infrastructure/AssemblyMarker.cs`
- `src/ScheduleAssistant.Infrastructure/Composition/InfrastructureServiceCollectionExtensions.cs`
- `src/ScheduleAssistant.Presentation/ScheduleAssistant.Presentation.csproj`
- `src/ScheduleAssistant.Presentation/App.xaml`
- `src/ScheduleAssistant.Presentation/App.xaml.cs`
- `src/ScheduleAssistant.Presentation/MainWindow.xaml`
- `src/ScheduleAssistant.Presentation/MainWindow.xaml.cs`

### 测试

- `tests/ScheduleAssistant.Domain.Tests/ScheduleAssistant.Domain.Tests.csproj`
- `tests/ScheduleAssistant.Domain.Tests/AssemblySmokeTests.cs`
- `tests/ScheduleAssistant.Application.Tests/ScheduleAssistant.Application.Tests.csproj`
- `tests/ScheduleAssistant.Application.Tests/AssemblySmokeTests.cs`
- `tests/ScheduleAssistant.Infrastructure.Tests/ScheduleAssistant.Infrastructure.Tests.csproj`
- `tests/ScheduleAssistant.Infrastructure.Tests/AssemblySmokeTests.cs`
- `tests/ScheduleAssistant.Architecture.Tests/ScheduleAssistant.Architecture.Tests.csproj`
- `tests/ScheduleAssistant.Architecture.Tests/DependencyDirectionTests.cs`

### 协作文档

- `docs/specifications/README.md`
- `docs/adr/README.md`
- `docs/adr/000-template.md`
- `docs/handoffs/DEV-xxx-template.md`
- `docs/test-reports/README.md`
- `docs/handoffs/DEV-001.md`

## 数据库/接口影响

- 未创建数据库、SQLite 文件、迁移 SQL、表、索引或数据目录。
- 未实现任何业务实体、用例、仓储、通知、托盘、自启动、单实例 IPC 或桌面宿主接口。
- 仅新增空项目边界、程序集 marker 和 `AddInfrastructure` 组合注册扩展；扩展当前不注册业务服务。
- 中央包版本为后续任务的工程基线，后续任务不得在项目文件中单独覆盖版本。

## 执行的验证命令与结果

### 已通过的静态检查

- XML/JSON 解析检查：通过；`Directory.Build.props`、`Directory.Packages.props`、`NuGet.config`、8 个 `.csproj` 和 WPF XAML 文件均可解析。
- 解决方案路径检查：通过；解决方案包含 8 个项目，全部项目路径存在。
- 项目引用路径检查：通过；8 个项目文件中的 `ProjectReference` 均指向存在的项目文件。
- 生产依赖图检查：通过；Domain 无生产项目依赖，Application 仅依赖 Domain，Infrastructure 仅依赖 Application/Domain，Presentation 仅依赖 Application/Infrastructure。
- CI 配置检查：通过；`.github/workflows/ci.yml` 包含 restore、Release build 和 test 步骤，并在 `windows-latest` 执行。
- DEV-001 范围检查：通过；`src`/`tests` 未发现数据库/迁移文件、`CREATE TABLE`、SQLite 连接或业务实现标记。

### 未能执行的运行时验证

以下命令均已实际尝试，但当前环境未安装 `dotnet`，PowerShell 返回“`dotnet` is not recognized”后未启动命令：

```powershell
dotnet restore .\ScheduleAssistant.sln --nologo
dotnet build .\ScheduleAssistant.sln --configuration Release --nologo
dotnet test .\ScheduleAssistant.sln --configuration Release --nologo
```

本机也未发现 `msbuild`、`csc` 或常见路径下的 `dotnet.exe`。因此以下结果必须在安装 .NET 10 SDK 的 Windows 环境补验：

- NuGet restore 是否成功；
- Release 编译是否无警告；
- 全部 xUnit 测试（含架构依赖测试）是否通过；
- WPF 启动页是否能运行；
- Windows 10/11 上的通知、托盘、单实例/IPC、自启动和桌面宿主专项验证。

## 未完成与已知问题

- 当前工作区没有 Git 元数据；初始 `git status --short --branch` 返回“not a git repository”，因此没有可提供的提交差异或 SHA。
- 未生成 `obj`、`bin` 或 `TestResults`，也没有伪造构建/测试通过结果。
- SPIKE-001 和 SPIKE-002 的 Windows 技术实验不属于本轮 DEV-001，ADR 目录目前只有模板和主题索引。

## 后续任务注意事项

1. 在 Windows + .NET 10 SDK 环境先按 README 执行 restore、Release build 和全量 test，并把实际结果补入测试报告。
2. 先完成 SPIKE-001/SPIKE-002 的验证与 ADR，再开始 DEV-010/DEV-020 等业务与数据任务。
3. 持久化任务必须新增不可变版本化迁移，并自行负责 SQLite/WAL/外键/事务测试；不要把 DEV-001 的空 Infrastructure 边界误认为已有数据层。
4. 后续 UI 任务应保留本轮最小启动页的组合根边界，不把具体基础设施注入 ViewModel。

## 最后提交 SHA

Not available (Git repository metadata is absent)

---

## 本轮 DEV-001 修复与验证补充（2026-09-16）

本节追加于原始交付记录之后。原记录中的“当时无 Git 元数据”和“当时未安装 dotnet”均为历史事实，保留不改写。

### 基线检查

- 从 `origin/main` 获取到提交 `20ec16506b037468a71e61e871c03f05b5c33bac`。
- 初始工作区干净；修复在独立分支 `fix/dev-001-build-validation` 上进行。
- 最新远程 CI 运行：[35082941357](https://github.com/LAlongago/ScheduleAssistant/actions/runs/35082941357) 在 Release build 阶段失败，日志确认了 Application `AssemblyMarker` 类型不匹配和测试 CA1707；测试阶段未启动。

### 本轮修复

- `ScheduleAssistant.Application/AssemblyMarker.cs`：将 `DomainAssembly` 属性类型从 `Type` 修正为 `Assembly`。
- `ScheduleAssistant.Infrastructure/AssemblyMarker.cs`：将 `ApplicationAssembly` 与 `DomainAssembly` 属性类型从 `Type` 修正为 `Assembly`。
- 新增 `tests/.editorconfig`，仅在测试目录将 CA1707 设为 `none`，保留行为式 xUnit 下划线命名；生产代码仍受 CA1707 约束。

### 本轮修改文件

- `src/ScheduleAssistant.Application/AssemblyMarker.cs`
- `src/ScheduleAssistant.Infrastructure/AssemblyMarker.cs`
- `tests/.editorconfig`
- `docs/test-reports/DEV-001-build-validation.md`
- `docs/handoffs/DEV-001.md`

### 本轮验证结果

- PowerShell 7：7.6.5；Windows：`Microsoft Windows 10.0.26200`；Git：2.55.0.windows.3。
- `global.json` 配置 SDK 10.0.100，但当前机器未安装 `dotnet`、`msbuild`、`csc`，也未发现常见路径下的 `dotnet.exe`。
- `dotnet --info`：退出码 1，命令未启动。
- `dotnet restore .\ScheduleAssistant.sln`：退出码 1，命令未启动。
- `dotnet build .\ScheduleAssistant.sln -c Release --no-restore`：退出码 1，命令未启动。
- `dotnet test .\ScheduleAssistant.sln -c Release --no-build --no-restore`：退出码 1，测试宿主未启动；测试统计为 0 discovered / 0 passed / 0 failed（阻塞，不是通过）。
- `dotnet run --project .\src\ScheduleAssistant.Presentation\ScheduleAssistant.Presentation.csproj -c Release --no-build`：退出码 1，WPF 进程未启动，因此窗口显示/关闭验证待补。
- 可执行环境之外的静态复核通过：项目/配置 XML 与 SDK JSON 可解析，生产依赖方向未改变，目标源码未新增业务、数据库或迁移实现，差异无 whitespace error。

### 当前结论

类型错误和测试分析器冲突已按最小范围修复，但由于当前缺少 SDK，本轮不能宣称 DEV-001 已完成真实构建、测试或 WPF 启动验收。需在安装 .NET 10 SDK 的 Windows 环境补跑完整命令，并单独确认 WPF 空窗口正常显示、关闭及进程退出。

### 后续依赖

1. 不安装系统级工具或修改全局配置的前提下，由具备 .NET 10 SDK 的 Windows 环境执行 `docs/test-reports/DEV-001-build-validation.md` 中的待补命令。
2. 仅当 restore、Release build、全量 test 和 WPF 启动均成功后，才可将 DEV-001 标记为可构建、可测试、可启动基线。
3. SPIKE-001/SPIKE-002 仍需按规格在对应 Windows 环境完成；本轮未实现任何通知、托盘、启动项、单实例 IPC 或桌面宿主。

### 本轮修复提交 SHA

`9521ce2` (`DEV-001: repair build validation baseline`)

---

## 本轮 DEV-001 环境补齐与真实验收追加（2026-09-16）

### 任务范围

- 任务编号：DEV-001 环境补齐与真实验收。
- 允许修改：DEV-001 启动/构建配置，以及 README、handoff、测试报告；本轮源代码只允许修复 Presentation 组合根的启动/架构验证问题。
- 明确不在范围：业务实体、用例、ViewModel、SQL、迁移、数据库、真实通知/托盘/自启动适配器、IPC、桌面宿主、UI 美化、SPIKE-001、SPIKE-002、推送、PR、合并 main。

### 完成内容与关键设计选择

- 按 Microsoft 官方 `dotnet-install.ps1` 在空的 `E:\Dev\Tools\ScheduleAssistantDotnet` 目录安装 Windows x64 .NET SDK 10.0.100；仅当前会话配置工具路径和可写临时 CLI/NuGet/AppData 目录，不修改系统 PATH、注册表、全局 Git 配置或其他 SDK。
- 真实 Release 编译暴露 `ScheduleAssistant.Application` 命名空间遮蔽 WPF `Application` 基类的问题；`App` 现在显式继承 `System.Windows.Application`。
- 真实架构测试暴露 Release 下被丢弃的 `typeof(ApplicationAssemblyMarker)` 未保留 Presentation 到 Application 的直接程序集引用；组合根现在读取 `ApplicationAssemblyMarker.DomainAssembly` 并在不可用时抛出启动错误，保留可验证的直接边界引用。
- 没有新增业务行为、数据库对象、Windows 集成实现或 UI 样式。

### 修改文件与影响

- `src/ScheduleAssistant.Presentation/App.xaml.cs`
- `README.md`
- `docs/test-reports/DEV-001-build-validation.md`
- `docs/handoffs/DEV-001.md`
- 数据库影响：无；未创建数据库、表、迁移、索引或数据目录。
- 公共接口影响：无；仅调整组合根类型限定和 DEV-001 程序集边界锚点。
- 生成的 `bin`、`obj`、`TestResults` 和本地安装脚本不纳入 Git 提交。

### 环境与真实验证

- Windows 10 Pro 25H2，`10.0.26200`（build `26200`），x64；PowerShell 7.6.5 Core。
- 实际 SDK：`E:\Dev\Tools\ScheduleAssistantDotnet\dotnet.exe`，10.0.100；MSBuild 18.0.2；`Microsoft.WindowsDesktop.App 10.0.0` 可用。
- `global.json` 在仓库目录实际选择 SDK 10.0.100；`dotnet --info`、`dotnet --list-sdks`、`dotnet --list-runtimes` 均退出码 0。
- `dotnet restore .\ScheduleAssistant.sln`：退出码 0，8 个项目均还原或为最新。
- `dotnet build .\ScheduleAssistant.sln -c Release --no-restore`：退出码 0，8 个项目生成，0 警告、0 错误。
- `dotnet test .\ScheduleAssistant.sln -c Release --no-build --no-restore --logger "trx" --results-directory .\TestResults`：退出码 0；4 个测试宿主均启动，7 executed、7 passed、0 failed、0 skipped。

测试项目统计：Domain 1/1、Application 1/1、Infrastructure 1/1、Architecture 4/4；TRX 结果均为 `Passed`。本轮首次测试曾真实启动四个宿主并得到 Architecture 1 项失败，修复后完整重跑通过；不存在“0 discovered”冒充通过的情况。

### WPF 验收状态

已执行：

```powershell
dotnet run --project .\src\ScheduleAssistant.Presentation\ScheduleAssistant.Presentation.csproj -c Release --no-build
```

该命令已启动外部 `ScheduleAssistant.exe`，进程路径和 `ScheduleAssistant` 主窗口标题/非零句柄已由进程检查确认；但当前原生窗口观察通道未配置（`sky` 报告 `Trusted RPC service is not configured`，`cua` 仅暴露浏览器），不能把进程存在当作窗口验收。以下五项在用户实际观察并关闭窗口前均保持“待人工验证”：

1. 主窗口确实显示；
2. 无启动异常或异常提示；
3. 可以正常关闭窗口；
4. 关闭后应用进程正常退出；
5. 没有残留后台应用进程。

### 未完成、已知问题与后续依赖

- WPF 五项桌面检查仍待用户在真实桌面完成并反馈；当前 `dotnet run` 会话保持运行以便观察。
- 远程 CI：待推送后验证；本轮未推送，不能宣称 GitHub CI 通过。
- 只有在 WPF 人工验收完成、分支推送并 CI 全绿且合入 main 后，才可从 main 基线启动 SPIKE-001/SPIKE-002 或后续业务任务。

### 新终端复现

```powershell
$dotnetRoot = 'E:\Dev\Tools\ScheduleAssistantDotnet'
$env:PATH = "$dotnetRoot;$env:PATH"
$env:DOTNET_ROOT = $dotnetRoot
$env:DOTNET_CLI_HOME = Join-Path $env:TEMP 'ScheduleAssistantDotnetCliHome'
$env:NUGET_PACKAGES = Join-Path $env:TEMP 'ScheduleAssistantNuGetPackages'
$env:APPDATA = Join-Path $env:TEMP 'ScheduleAssistantAppData'
Set-Location 'E:\Dev\Personal\Todo_list'
dotnet --info
dotnet restore .\ScheduleAssistant.sln
dotnet build .\ScheduleAssistant.sln -c Release --no-restore
dotnet test .\ScheduleAssistant.sln -c Release --no-build --no-restore --logger "trx" --results-directory .\TestResults
dotnet run --project .\src\ScheduleAssistant.Presentation\ScheduleAssistant.Presentation.csproj -c Release --no-build
```

### 最后提交 SHA

本轮代码与验证文档提交：`1f4766a7caec7b66e90ae65fac7d09190ba0d94c`
（`DEV-001: validate local SDK and WPF build`）。后续若仅补录人工 WPF 反馈，另行记录追加提交。

---

## 人工 WPF 五项验收反馈追加（2026-09-17）

上一节记录的“待人工验证”状态及原生观察通道限制均为当时真实事实，保留不改写。用户随后在真实 Windows 桌面完成了五项检查并反馈全部通过；`dotnet run` 退出码为 `0`。

### 验收结果

1. 主窗口确实显示：通过；
2. 无启动异常或异常提示：通过；
3. 可以正常关闭窗口：通过；
4. 关闭后应用进程正常退出：通过，退出码 `0`；
5. 没有残留后台应用进程：通过；关闭后独立 `Get-Process -Name ScheduleAssistant` 检查无输出。

### 更新后的结论

- 环境准备、Release 构建、全量测试和 WPF 运行验收均通过。
- 远程 CI 仍为“待推送后验证”；当前分支未推送，不能宣称 GitHub CI 通过。
- DEV-001 本地验证门禁已完成；下一步可在获得授权后推送当前分支并验证 CI。
- 在分支 CI 全绿且合入 `main` 前，不启动 SPIKE-001、SPIKE-002 或后续业务任务。

### 追加记录提交

本节应随人工验收反馈文档提交一并交付；最新 Git SHA 以交付时的 `git rev-parse HEAD` 为准。

---

## 推送与远程 CI 状态追加（2026-09-17）

用户已明确授权推送到 `https://github.com/LAlongago/ScheduleAssistant.git`，并在 CI 全绿后继续合入 `main`。本地分支当时干净，HEAD 为 `e0ac2c560e14cbfbdd7ac6b5ee146fbc0887bc86`。

- 第一次 `git push --set-upstream origin fix/dev-001-build-validation`：退出码 `1`，HTTPS 连接被重置。
- 第二次相同推送：退出码 `1`，约 21 秒后报告无法连接 `github.com:443`。
- 只读 `curl.exe --fail --silent --show-error --location --head https://github.com/LAlongago/ScheduleAssistant`：退出码 `1`，同样无法连接 `github.com:443`。
- 未出现认证提示、远程拒绝或分支冲突；未观察到远程引用更新，CI 尚未启动，合并尚未执行。

当前阻塞是外部 GitHub HTTPS 网络不可达，不是代码、构建或测试失败。恢复 GitHub 网络后重试推送，检查完整 CI；只有全部必需检查通过才继续合入 `main`。
