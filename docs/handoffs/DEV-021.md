# DEV-021 交付记录

## 任务范围

- 任务编号：`DEV-021`。
- 分支：`feat/dev-021-settings-logging`。
- 独立 worktree：`E:\Dev\Personal\Todo_list\.worktrees\dev-021-settings-logging`。
- 允许范围：Application 配置抽象与设置模型；Infrastructure 的 AppPaths、Settings、Logging、组合注册；对应 Infrastructure 测试；中央 Logging.Abstractions 包条目；本 handoff。
- 明确不在范围：Domain、仓储端口、迁移 SQL、Presentation、CI、注册表自启动、托盘、提醒调度、备份、DST 和相关 Windows 功能实现。

## 完成内容

- 新增 `IAppPaths`，由现有 `AppPaths` 实现；保留原有根目录、data、attachments、backups、logs、settings 布局，并增加 `settings/user-settings.json` 路径。DI 将同一 `AppPaths` 实例同时暴露为 concrete 和 `IAppPaths`。
- 新增 `UserSettings`、`IUserSettingsStore` 和可诊断的加载/保存结果。默认值为：系统主题、关闭到托盘、自启动关闭、默认提醒开启、提醒偏移 `-1440` 分钟、桌面模式关闭。
- `JsonUserSettingsStore` 对缺失文件返回默认值；损坏、缺失/不合法版本号、未知版本和读取失败保留原文件并返回状态/诊断码。保存使用 settings 目录内临时文件和原子替换；替换失败保留旧文件，缓存只在替换成功后更新。
- 新增异步有界文件 Logger Provider：日志写入现有 `LogsDirectory`，按 UTC 日期和大小滚动，默认最多保留 14 个本 Provider 文件，关闭时有限等待队列刷新；文件系统失败被隔离，不递归记录 Provider 自身错误。
- 日志只输出 UTC、级别、事件编号、技术类别、白名单技术字段、异常类型和 HResult；不调用 formatter、不保存 scope、自由文本、任务标题/正文/材料、附件路径或未经筛选的异常消息。
- 完成 Infrastructure DI 注册，不在注册或构造阶段初始化目录、设置文件或数据库。

## 修改文件

- `src/ScheduleAssistant.Application/Abstractions/Configuration/IAppPaths.cs`
- `src/ScheduleAssistant.Application/Abstractions/Settings/` 下设置模型、端口和结果类型
- `src/ScheduleAssistant.Infrastructure/Persistence/AppPaths.cs`
- `src/ScheduleAssistant.Infrastructure/Settings/JsonUserSettingsStore.cs`
- `src/ScheduleAssistant.Infrastructure/Logging/`
- `src/ScheduleAssistant.Infrastructure/Composition/InfrastructureServiceCollectionExtensions.cs`
- `src/ScheduleAssistant.Infrastructure/ScheduleAssistant.Infrastructure.csproj`
- `Directory.Packages.props`
- `tests/ScheduleAssistant.Infrastructure.Tests/SettingsAndLoggingTests.cs`

## 数据库/接口影响

- 未修改 Domain、仓储端口、迁移 SQL、数据库表或持久化编码。
- 新增 Application 配置/设置抽象；新增 Infrastructure JSON 设置适配器和文件日志 Provider。
- 仅新增中央包条目及 Infrastructure 引用：`Microsoft.Extensions.Logging.Abstractions 10.0.0`；未升级已有包。
- 设置值只表达用户意愿，不表示自启动、托盘、提醒或桌面模式已经生效。

## 执行的验证命令与结果

按要求执行：

```powershell
dotnet build .\ScheduleAssistant.sln -c Release -p:NuGetAudit=true -p:NuGetAuditMode=all
dotnet test .\tests\ScheduleAssistant.Infrastructure.Tests\ScheduleAssistant.Infrastructure.Tests.csproj -c Release --no-build --no-restore
git diff --check
```

- Release solution build：通过，`0 warnings / 0 errors`。
- Infrastructure tests：通过，`16 passed / 0 failed / 0 skipped`。
- `git diff --check`：通过。
- 构建使用 README 指定的 `E:\Dev\Tools\ScheduleAssistantDotnet` 10.0.100 SDK；首次受限进程 restore 遇到 NuGet TLS 错误，随后按环境要求允许网络访问后 restore/build 成功。

## 未完成与已知问题

- 本轮不实现设置页面及任何 Windows 自启动、托盘、通知、桌面宿主或提醒调度；后续任务读取设置意愿并接入对应适配器。
- 未运行本地全量测试、性能压测或 WPF/SPIKE 验收，符合任务要求。
- Provider 的默认滚动/保留参数可由 Infrastructure 组合配置覆盖；日志写入失败按 fail-closed 处理，不影响业务调用。

## 后续任务注意事项

- Presentation 组合根可直接解析 `IUserSettingsStore`、`IAppPaths` 和 `ILogger<T>`；读取设置应处理非 `Loaded` 状态并向用户提供恢复路径，不应覆盖原文件。
- 后续 Windows 集成只能把 `StartupEnabled`、`CloseBehavior`、`DesktopModeEnabled` 和提醒默认值解释为意愿，不能在本基础设施层提前执行系统副作用。
- 事务/数据库初始化仍必须由显式入口触发，不能借助本轮 DI 注册或日志初始化隐式创建。

## 最后提交 SHA

- DEV-021 实现提交：`8e067fd`（`DEV-021: implement settings and logging foundation`）。
- handoff 追加提交的完整 SHA 在最终交付消息中报告；不将会改变自身内容的自引用 SHA 写入本文件。
